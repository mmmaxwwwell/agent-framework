You are modifying my project to integrate three new MCP tools into the spec-kit system with the same local-first, flake-consumable, lifecycle-managed pattern already used for code-review-graph.Project context:- Repo contains `.claude/skills/spec-kit/SKILL.md`- Repo contains `.claude/skills/spec-kit/parallel_runner.py`- There is already an existing pattern for how `code-review-graph` is:  - described in the skill  - installed/started locally  - treated as a required first-class dependency  - checked for readiness  - used by agents through explicit instructions  - kept running or started on demand- I want the same pattern applied to:  1. Grounded Docs / `docs-mcp-server`  2. Serena  3. Semgrep MCPGoal:Make these three tools consumable from a Nix flake in the same spirit as the code graph server integration, and wire the spec-kit wrapper skill + runner so agents know how to install, start, verify, use, and maintain them through the task lifecycle.High-level requirements:1. Do not redesign the existing architecture.2. Follow the exact style, conventions, and intent already present in the `code-review-graph` integration.3. Treat these tools as locally managed dependencies that the wrapper can provision and expose.4. Add only what is necessary to make agents effective and disciplined in using them.5. Preserve the current graph-first workflow; do not replace code-review-graph.6. Make all changes concrete and directly usable.What I want you to produce:1. A patch to `.claude/skills/spec-kit/SKILL.md`2. A patch to `.claude/skills/spec-kit/parallel_runner.py`3. Any additional helper/config files that are necessary to support the integration pattern4. A short explanation of the design choices5. Commands/examples showing how the lifecycle works6. If appropriate, a suggested flake-facing contract for how the wrapper should provide these toolsImplementation intent:Replicate the “code graph server” pattern for each new tool:A. Tool provisioning / local consumption- Assume the outer Nix flake wrapper is responsible for making the binaries/services available locally.- Add clear assumptions/contracts for how the wrapper exposes each tool, similar in spirit to how code-review-graph is expected to exist/be started.- Prefer environment-variable-driven or explicit executable-path-driven integration if that matches the current pattern.- Do not hardcode brittle absolute paths.- Make the integration easy for a local flake devShell / app / package to satisfy.B. Tool lifecycle management- Add logic and instructions so the spec-kit system can:  - detect whether each tool is available  - install or bootstrap it if the current design already allows that pattern; otherwise emit a precise BLOCKED state with actionable guidance  - start it if needed  - verify it is healthy/ready  - restart or reinitialize it when stale/broken  - avoid duplicate unmanaged background processes- Follow the same lifecycle philosophy already used for code-review-graph.C. Agent instructionsUpdate the skill so agents know:- when to use Grounded Docs- when to use Serena- when to use Semgrep MCP- what order to use them in relative to code-review-graph- how to avoid overusing them- what to do if a tool is unavailable or unhealthy- how to capture evidence/findings when a tool produces important outputDesired routing policy:- `code-review-graph` remains first for local code understanding / blast-radius / minimal context- `Grounded Docs (docs-mcp-server)` is for external library/framework/package documentation and version-specific API grounding- `Serena` is for symbol-aware navigation/edit/refactor after graph-based scoping identifies the right area- `Semgrep MCP` is for security scanning / validation / deterministic security feedback, especially in validate/fix loops- Agents should not substitute docs lookups for repo understanding, and should not use Semgrep as a replacement for normal validation or testsD. Style/behavior requirements for SKILL.mdAdd instructions in the same tone and level of specificity as the existing skill.Include:- required/strongly-preferred usage patterns- anti-patterns- fallback behavior- lifecycle expectations- explicit “use X before Y” guidance where appropriate- concise operational steps agents can follow without improvisingE. Style/behavior requirements for `parallel_runner.py`Extend the runner so it has first-class awareness of these tools similarly to code-review-graph.This likely includes things like:- capability registration / dependency awareness- readiness checks- startup hooks or management helpers- failure reporting and actionable messages- propagation into spawned agents / instructions- preserving existing safety and capability controlsF. Flake-facing contractPropose a simple, explicit contract for the outer flake wrapper to satisfy, for example:- environment variables- executable names expected on PATH- optional cache/data dirs- optional config dirs- per-tool startup commands- healthcheck commandsKeep this contract minimal and consistent.Specific expectations per tool:1. Grounded Docs / docs-mcp-server- Add instructions for using it to retrieve authoritative external docs before guessing about third-party APIs/frameworks- Emphasize version-aware or source-aware lookups where possible- Make agents prefer it over ad hoc web wandering when the question is about external library usage- Add lifecycle assumptions for local availability through the wrapper- Include fallback behavior if docs service is unavailable2. Serena- Add instructions for using it after code-review-graph has scoped the relevant code region- Emphasize symbol-level navigation, precise edits, refactors, and structural understanding- Discourage broad file wandering when Serena can answer more directly- Add lifecycle/startup/readiness logic consistent with the rest of the system3. Semgrep MCP- Add instructions for deterministic security review and validation- Integrate into validate/fix loops without making it mandatory for every trivial task unless that matches existing philosophy- Clarify when Semgrep findings should block progress vs be reported as findings- Add lifecycle/startup/readiness logic- Ensure the behavior fits the existing BLOCKED/findings modelImportant constraints:- Do not invent a new framework- Do not remove or weaken existing code-review-graph behavior- Do not make agent instructions vague- Do not rely on manual human intervention unless absolutely necessary- If something cannot be fully automated, encode precise BLOCKED instructions and recovery steps- Keep the changes idiomatic to the current codebaseHow to work:1. Read the existing `SKILL.md`2. Read the existing `parallel_runner.py`3. Infer the current code-review-graph integration pattern4. Mirror that pattern for:   - Grounded Docs / docs-mcp-server   - Serena   - Semgrep MCP5. Produce concrete edits, not just suggestionsOutput format:- Show unified diffs or full replacement blocks for each modified file- Include any new helper code/files- Then include a concise explanation of:  - lifecycle model  - agent routing order  - flake contract  - any assumptions that must be satisfied by the wrapperQuality bar:I should be able to take your output, apply the edits, wire the wrapper to satisfy the contract, and have agents use these three tools in the same disciplined, managed way as code-review-graph.
